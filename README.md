# 🚀 RuStore Parser

Умный парсер, который анализирует топ-приложения из Google Play (AppBrain) и находит возможности для клонирования в RuStore.

## Локальная разработка

```bash
docker compose up --build
```

Приложение будет доступно на `http://localhost:5001`

## Деплой на сервер (production)

### Первоначальная настройка

1. **Авторизоваться в GHCR на сервере:**
```bash
echo "YOUR_GITHUB_PAT" | docker login ghcr.io -u YOUR_USERNAME --password-stdin
```

2. **Создать Cloudflare Tunnel** в [Zero Trust Dashboard](https://one.dash.cloudflare.com) и получить токен.

3. **Скопировать файлы на сервер:**
```bash
scp docker-compose.prod.yml .env user@server:~/rustore-parser/
```

4. **Создать `.env` на сервере:**
```bash
cp .env.example .env
# Вписать CLOUDFLARE_TUNNEL_TOKEN
```

5. **Запустить:**
```bash
docker compose -f docker-compose.prod.yml up -d
```

### Как работает автодеплой

1. Вы пушите в `main` → GitHub Actions собирает Docker-образ → пушит в GHCR
2. Watchtower на сервере проверяет новые образы каждые 60 сек
3. Обнаружив обновление, Watchtower автоматически пулит и перезапускает контейнер
4. Cloudflare Tunnel обеспечивает постоянную публичную ссылку

### Для контрибьюторов

1. Форкните репозиторий
2. Создайте feature-ветку: `git checkout -b feature/my-feature`
3. Закоммитьте: `git commit -m "feat: описание"`
4. Запушьте: `git push origin feature/my-feature`
5. Откройте Pull Request в `main`
