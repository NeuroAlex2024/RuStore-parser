# 🚀 RuStore Parser

Умный парсер, который анализирует топ-приложения из Google Play (AppBrain) и находит возможности для клонирования в RuStore. Определяет **Opportunity Score** — насколько привлекательно приложение для клонирования, учитывая наличие и качество существующих решений в RuStore.

## Архитектура

```
GitHub (main) → GitHub Actions → GHCR → Watchtower → Сервер → Cloudflare Tunnel → 🌍
```

| Компонент | Роль |
|-----------|------|
| **GitHub Actions** | Собирает Docker-образ при push в `main` |
| **GHCR** | Хранит Docker-образы |
| **Watchtower** | Автоматически обновляет контейнер на сервере (каждые 60 сек) |
| **Cloudflare Tunnel** | Предоставляет публичную HTTPS-ссылку |

## Локальная разработка

```bash
docker compose up --build
```

Приложение будет доступно на `http://localhost:5001`

## Workflow разработки

### Основной разработчик

```bash
# 1. Сделал фичу, протестил локально
docker compose up --build
# Проверил на http://localhost:5001

# 2. Пушишь в main — деплой автоматический
git add .
git commit -m "feat: описание фичи"
git push origin main

# 3. Через ~3 минуты фича в продакшене
```

### Контрибьютор

1. Форкните репозиторий
2. Клонируйте свой форк:
   ```bash
   git clone https://github.com/YOUR_USERNAME/RuStore-parser.git
   cd RuStore-parser
   docker compose up --build  # локальная разработка на localhost:5001
   ```
3. Создайте feature-ветку: `git checkout -b feature/my-feature`
4. Сделайте изменения, протестируйте локально
5. Закоммитьте: `git commit -m "feat: описание"`
6. Запушьте: `git push origin feature/my-feature`
7. Откройте **Pull Request** в `main`

## Деплой на сервер (production)

### Первоначальная настройка

1. **Авторизоваться в GHCR на сервере:**
   ```bash
   echo "YOUR_GITHUB_PAT" | docker login ghcr.io -u YOUR_USERNAME --password-stdin
   ```

2. **Скопировать файлы на сервер:**
   ```bash
   scp docker-compose.prod.yml user@server:~/rustore-parser/
   ```

3. **Запустить:**
   ```bash
   cd ~/rustore-parser
   docker compose up -d
   ```

### Как работает автодеплой

1. Push в `main` → GitHub Actions собирает Docker-образ → пушит в GHCR
2. Watchtower на сервере проверяет новые образы каждые 60 сек
3. Обнаружив обновление — автоматически pull + restart контейнера
4. Cloudflare Tunnel не трогается, ссылка остаётся прежней

### Полезные команды

| Задача | Команда |
|--------|---------|
| Статус CI | `gh run list` |
| Логи сборки | `gh run view --log` |
| Логи приложения | `docker logs rustore-parser` |
| Логи Watchtower | `docker logs watchtower-rustore` |
| Откат | Revert коммит → push → автодеплой |

## Технологии

- **Node.js** + **Playwright** (парсинг)
- **SQLite** (хранение данных)
- **Docker** + **GitHub Actions** + **Watchtower** (CI/CD)
- **Cloudflare Tunnel** (публичный доступ)
